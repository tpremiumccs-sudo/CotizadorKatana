node_modules/
.next/
out/
dist/
build/

# El seed empaquetado: lo genera esbuild dentro de la imagen, no se versiona.
/prisma/seed.mjs

# Entorno y secretos — NUNCA versionar
.env
.env.local
.env.*.local
!.env.example

# Compose local: no se versiona para que un `git pull` no re-exponga puertos
docker-compose.override.yml

# Artefactos de pruebas
/test-results/
/playwright-report/
/blob-report/
/.auth/
coverage/
tests/**/__salida__/

# Respaldos
/respaldos/
# Los .xlsx subidos: son datos comerciales reales de la agencia, no código.
/var/

# Los .xlsx del CRM: el tarifario completo, el roster y las notas comerciales
# internas. Se dejaron de versionar; siguen en el historial de git, así que hay
# que asumir que pudieron copiarse mientras el repositorio estuvo público.
# Quien los tenga los deja aquí y las pruebas de importación vuelven a correr.
/tests/fixtures/xlsx/

# Sistema
.DS_Store
*.log
npm-debug.log*
.idea/
.vscode/
next-env.d.ts
tsconfig.tsbuildinfo
__pycache__/
.venv/
/test-results/
/playwright-report/
